name: CI/CD Pipeline

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  secrets-scan:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Gitleaks Analysis
        run: |
          docker run --rm \
            --volume "$PWD:/repo" \
            --workdir /repo \
            ghcr.io/gitleaks/gitleaks:v8.18.2 \
            detect --source /repo --verbose --redact || true

  test-backend-SecurityController:
    runs-on: self-hosted 
    env:
      APP_ENV: test
      APP_SECRET: "ceci_est_un_secret_de_test_12345"
      DATABASE_URL: "sqlite:///${{ runner.temp }}/overkill-${{ github.job }}-${{ github.run_id }}-${{ github.run_attempt }}.db"
      DEFAULT_URI: "http://localhost"
      MESSENGER_TRANSPORT_DSN: "in-memory://"
      MAILER_DSN: "null://null"
      JWT_SECRET_KEY: "%kernel.project_dir%/config/jwt/private.pem"
      JWT_PUBLIC_KEY: "%kernel.project_dir%/config/jwt/public.pem"
      JWT_PASSPHRASE: "jwt_test_passphrase"
    steps:
      - uses: actions/checkout@v4

      # Configurer PHP sur ton runner local
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, iconv, intl, pdo_sqlite, pdo_mysql
          coverage: none

      # Installation sans exécuter les scripts automatiques
      - name: Install Dependencies
        working-directory: ./Overkill_Api
        run: composer install --no-progress --prefer-dist --no-scripts

      # Génération des clés JWT pour la suite de tests
      - name: Generate SSL Keys for JWT
        working-directory: ./Overkill_Api
        run: |
          mkdir -p config/jwt
          openssl genpkey -out config/jwt/private.pem -aes256 -algorithm rsa -pkeyopt rsa_keygen_bits:4096 -pass pass:jwt_test_passphrase
          openssl pkey -in config/jwt/private.pem -passin pass:jwt_test_passphrase -pubout -out config/jwt/public.pem

      # S'assurer que .env existe et contient l'intégralité des variables de configuration
      - name: Ensure .env file exists with defaults
        working-directory: ./Overkill_Api
        run: |
          if [ -f .env.test ]; then
            cp .env.test .env
          else
            echo "APP_ENV=test" > .env
            echo "APP_SECRET=ceci_est_un_secret_de_test_12345" >> .env
            echo "DATABASE_URL=$DATABASE_URL" >> .env
            echo "DEFAULT_URI=http://localhost" >> .env
            echo "MESSENGER_TRANSPORT_DSN=in-memory://" >> .env
            echo "MAILER_DSN=null://null" >> .env
            echo "JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem" >> .env
            echo "JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem" >> .env
            echo "JWT_PASSPHRASE=jwt_test_passphrase" >> .env
          fi

      # Réinitialiser la base de données et charger les Fixtures de test
      - name: Prepare Database for Tests
        working-directory: ./Overkill_Api
        run: |
          php bin/console doctrine:schema:update --force --env=test
          php bin/console doctrine:fixtures:load --no-interaction --env=test || true

      # Exécuter PHPUnit spécifiquement pour SecurityControllerTest
      - name: Run SecurityController Tests
        working-directory: ./Overkill_Api
        run: vendor/bin/phpunit tests/Controller/SecurityControllerTest.php

  test-backend-CvController:
    runs-on: self-hosted 
    env:
      APP_ENV: test
      APP_SECRET: "ceci_est_un_secret_de_test_12345"
      DATABASE_URL: "sqlite:///${{ runner.temp }}/overkill-${{ github.job }}-${{ github.run_id }}-${{ github.run_attempt }}.db"
      DEFAULT_URI: "http://localhost"
      MESSENGER_TRANSPORT_DSN: "in-memory://"
      MAILER_DSN: "null://null"
      JWT_SECRET_KEY: "%kernel.project_dir%/config/jwt/private.pem"
      JWT_PUBLIC_KEY: "%kernel.project_dir%/config/jwt/public.pem"
      JWT_PASSPHRASE: "jwt_test_passphrase"
    steps:
      - uses: actions/checkout@v4

      # Configurer PHP sur ton runner local
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, iconv, intl, pdo_sqlite, pdo_mysql
          coverage: none

      # Installation sans exécuter les scripts automatiques
      - name: Install Dependencies
        working-directory: ./Overkill_Api
        run: composer install --no-progress --prefer-dist --no-scripts

      # Génération des clés JWT pour la suite de tests
      - name: Generate SSL Keys for JWT
        working-directory: ./Overkill_Api
        run: |
          mkdir -p config/jwt
          openssl genpkey -out config/jwt/private.pem -aes256 -algorithm rsa -pkeyopt rsa_keygen_bits:4096 -pass pass:jwt_test_passphrase
          openssl pkey -in config/jwt/private.pem -passin pass:jwt_test_passphrase -pubout -out config/jwt/public.pem

      # S'assurer que .env existe
      - name: Ensure .env file exists with defaults
        working-directory: ./Overkill_Api
        run: |
          if [ -f .env.test ]; then
            cp .env.test .env
          else
            echo "APP_ENV=test" > .env
            echo "APP_SECRET=ceci_est_un_secret_de_test_12345" >> .env
            echo "DATABASE_URL=$DATABASE_URL" >> .env
            echo "DEFAULT_URI=http://localhost" >> .env
            echo "MESSENGER_TRANSPORT_DSN=in-memory://" >> .env
            echo "MAILER_DSN=null://null" >> .env
            echo "JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem" >> .env
            echo "JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem" >> .env
            echo "JWT_PASSPHRASE=jwt_test_passphrase" >> .env
          fi

      # Réinitialiser la base de données
      - name: Prepare Database for Tests
        working-directory: ./Overkill_Api
        run: |
          php bin/console doctrine:schema:update --force --env=test
          php bin/console doctrine:fixtures:load --no-interaction --env=test || true

      # Exécuter PHPUnit spécifiquement pour CvControllerTest
      - name: Run CvController Tests
        working-directory: ./Overkill_Api
        run: vendor/bin/phpunit tests/Controller/CvControllerTest.php

  test-backend-OffersController:
    runs-on: self-hosted 
    env:
      APP_ENV: test
      APP_SECRET: "ceci_est_un_secret_de_test_12345"
      DATABASE_URL: "sqlite:///${{ runner.temp }}/overkill-${{ github.job }}-${{ github.run_id }}-${{ github.run_attempt }}.db"
      DEFAULT_URI: "http://localhost"
      MESSENGER_TRANSPORT_DSN: "in-memory://"
      MAILER_DSN: "null://null"
      JWT_SECRET_KEY: "%kernel.project_dir%/config/jwt/private.pem"
      JWT_PUBLIC_KEY: "%kernel.project_dir%/config/jwt/public.pem"
      JWT_PASSPHRASE: "jwt_test_passphrase"
    steps:
      - uses: actions/checkout@v4

      # Configurer PHP sur ton runner local
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, iconv, intl, pdo_sqlite, pdo_mysql
          coverage: none

      # Installation sans exécuter les scripts automatiques
      - name: Install Dependencies
        working-directory: ./Overkill_Api
        run: composer install --no-progress --prefer-dist --no-scripts

      # Génération des clés JWT pour la suite des tests
      - name: Generate SSL Keys for JWT
        working-directory: ./Overkill_Api
        run: |
          mkdir -p config/jwt
          openssl genpkey -out config/jwt/private.pem -aes256 -algorithm rsa -pkeyopt rsa_keygen_bits:4096 -pass pass:jwt_test_passphrase
          openssl pkey -in config/jwt/private.pem -passin pass:jwt_test_passphrase -pubout -out config/jwt/public.pem

      # S'assurer que .env existe
      - name: Ensure .env file exists with defaults
        working-directory: ./Overkill_Api
        run: |
          if [ -f .env.test ]; then
            cp .env.test .env
          else
            echo "APP_ENV=test" > .env
            echo "APP_SECRET=ceci_est_un_secret_de_test_12345" >> .env
            echo "DATABASE_URL=$DATABASE_URL" >> .env
            echo "DEFAULT_URI=http://localhost" >> .env
            echo "MESSENGER_TRANSPORT_DSN=in-memory://" >> .env
            echo "MAILER_DSN=null://null" >> .env
            echo "JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem" >> .env
            echo "JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem" >> .env
            echo "JWT_PASSPHRASE=jwt_test_passphrase" >> .env
          fi

      # Réinitialiser la base de données
      - name: Prepare Database for Tests
        working-directory: ./Overkill_Api
        run: |
          php bin/console doctrine:schema:update --force --env=test
          php bin/console doctrine:fixtures:load --no-interaction --env=test || true

      # Exécuter PHPUnit spécifiquement pour OffersControllerTest
      - name: Run OffersController Tests
        working-directory: ./Overkill_Api
        run: vendor/bin/phpunit tests/Controller/OffersControllerTest.php

  test-backend-UserSkillController:
    runs-on: self-hosted 
    env:
      APP_ENV: test
      APP_SECRET: "ceci_est_un_secret_de_test_12345"
      DATABASE_URL: "sqlite:///${{ runner.temp }}/overkill-${{ github.job }}-${{ github.run_id }}-${{ github.run_attempt }}.db"
      DEFAULT_URI: "http://localhost"
      MESSENGER_TRANSPORT_DSN: "in-memory://"
      MAILER_DSN: "null://null"
      JWT_SECRET_KEY: "%kernel.project_dir%/config/jwt/private.pem"
      JWT_PUBLIC_KEY: "%kernel.project_dir%/config/jwt/public.pem"
      JWT_PASSPHRASE: "jwt_test_passphrase"
    steps:
      - uses: actions/checkout@v4

      # Configurer PHP sur ton runner local
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, iconv, intl, pdo_sqlite, pdo_mysql
          coverage: none

      # Installation sans exécuter les scripts automatiques
      - name: Install Dependencies
        working-directory: ./Overkill_Api
        run: composer install --no-progress --prefer-dist --no-scripts

      # Génération des clés JWT pour la suite des tests
      - name: Generate SSL Keys for JWT
        working-directory: ./Overkill_Api
        run: |
          mkdir -p config/jwt
          openssl genpkey -out config/jwt/private.pem -aes256 -algorithm rsa -pkeyopt rsa_keygen_bits:4096 -pass pass:jwt_test_passphrase
          openssl pkey -in config/jwt/private.pem -passin pass:jwt_test_passphrase -pubout -out config/jwt/public.pem

      # S'assurer que .env existe
      - name: Ensure .env file exists with defaults
        working-directory: ./Overkill_Api
        run: |
          if [ -f .env.test ]; then
            cp .env.test .env
          else
            echo "APP_ENV=test" > .env
            echo "APP_SECRET=ceci_est_un_secret_de_test_12345" >> .env
            echo "DATABASE_URL=$DATABASE_URL" >> .env
            echo "DEFAULT_URI=http://localhost" >> .env
            echo "MESSENGER_TRANSPORT_DSN=in-memory://" >> .env
            echo "MAILER_DSN=null://null" >> .env
            echo "JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem" >> .env
            echo "JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem" >> .env
            echo "JWT_PASSPHRASE=jwt_test_passphrase" >> .env
          fi

      # Réinitialiser la base de données
      - name: Prepare Database for Tests
        working-directory: ./Overkill_Api
        run: |
          php bin/console doctrine:schema:update --force --env=test
          php bin/console doctrine:fixtures:load --no-interaction --env=test || true

      # Exécuter PHPUnit spécifiquement pour UserSkillControllerTest
      - name: Run UserSkillController Tests
        working-directory: ./Overkill_Api
        run: vendor/bin/phpunit tests/Controller/UserSkillControllerTest.php

  test-backend-UserFavoriteController:
    runs-on: self-hosted 
    env:
      APP_ENV: test
      APP_SECRET: "ceci_est_un_secret_de_test_12345"
      DATABASE_URL: "sqlite:///${{ runner.temp }}/overkill-${{ github.job }}-${{ github.run_id }}-${{ github.run_attempt }}.db"
      DEFAULT_URI: "http://localhost"
      MESSENGER_TRANSPORT_DSN: "in-memory://"
      MAILER_DSN: "null://null"
      JWT_SECRET_KEY: "%kernel.project_dir%/config/jwt/private.pem"
      JWT_PUBLIC_KEY: "%kernel.project_dir%/config/jwt/public.pem"
      JWT_PASSPHRASE: "jwt_test_passphrase"
    steps:
      - uses: actions/checkout@v4

      # Configurer PHP sur ton runner local
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, iconv, intl, pdo_sqlite, pdo_mysql
          coverage: none

      # Installation sans exécuter les scripts automatiques
      - name: Install Dependencies
        working-directory: ./Overkill_Api
        run: composer install --no-progress --prefer-dist --no-scripts

      # Génération des clés JWT pour la suite des tests
      - name: Generate SSL Keys for JWT
        working-directory: ./Overkill_Api
        run: |
          mkdir -p config/jwt
          openssl genpkey -out config/jwt/private.pem -aes256 -algorithm rsa -pkeyopt rsa_keygen_bits:4096 -pass pass:jwt_test_passphrase
          openssl pkey -in config/jwt/private.pem -passin pass:jwt_test_passphrase -pubout -out config/jwt/public.pem

      # S'assurer que .env existe
      - name: Ensure .env file exists with defaults
        working-directory: ./Overkill_Api
        run: |
          if [ -f .env.test ]; then
            cp .env.test .env
          else
            echo "APP_ENV=test" > .env
            echo "APP_SECRET=ceci_est_un_secret_de_test_12345" >> .env
            echo "DATABASE_URL=$DATABASE_URL" >> .env
            echo "DEFAULT_URI=http://localhost" >> .env
            echo "MESSENGER_TRANSPORT_DSN=in-memory://" >> .env
            echo "MAILER_DSN=null://null" >> .env
            echo "JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem" >> .env
            echo "JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem" >> .env
            echo "JWT_PASSPHRASE=jwt_test_passphrase" >> .env
          fi

      # Réinitialiser la base de données
      - name: Prepare Database for Tests
        working-directory: ./Overkill_Api
        run: |
          php bin/console doctrine:schema:update --force --env=test
          php bin/console doctrine:fixtures:load --no-interaction --env=test || true

      # Exécuter PHPUnit spécifiquement pour UserFavoriteControllerTest
      - name: Run UserFavoriteController Tests
        working-directory: ./Overkill_Api
        run: vendor/bin/phpunit tests/Controller/UserFavoriteControllerTest.php
